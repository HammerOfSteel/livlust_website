# Copy this file to .env and fill in values
POSTGRES_USER=livlust
POSTGRES_PASSWORD=changeme
POSTGRES_DB=livlust_db

DATABASE_URL=postgresql://livlust:changeme@db:5432/livlust_db

JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=8h

# Initial superadmin (used by db seed script)
SEED_ADMIN_EMAIL=admin@livlust.se
SEED_ADMIN_PASSWORD=changeme

# Contact form: where to forward submissions (optional)
CONTACT_EMAIL=info@livlust.se

VITE_API_URL=http://localhost:4000
